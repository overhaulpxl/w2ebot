version: '3.8'

services:
  w2ebot:
    build: .
    container_name: w2ebot-cluster
    restart: unless-stopped
    ports:
      - "8080:8080"
      - "8082:8082"
    volumes:
      - ./w2ebot.db:/app/w2ebot.db
      - ./premium_users.json:/app/premium_users.json
    env_file:
      - .env
